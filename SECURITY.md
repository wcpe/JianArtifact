# 安全说明：JianArtifact

> 信任模型、敏感数据处理、外部输入校验与漏洞报告渠道。安全模型变化时更新。

## 1. 信任模型

- **可信**：管理员、持有效会话 / API Token 的已认证主体（在其 ACL 授权范围内）、运行实例的宿主环境。
- **不可信**：未认证请求、原生包客户端提交的路径 / 坐标 / 元数据、proxy 上游返回的内容、Nexus 迁移来源数据。
- **信任边界**：所有鉴权与 ACL 判定在后端完成，前端不承担授权（不变量）。协议端点与管理 API 均经统一鉴权中间件。
- **攻击面**：协议端点（路径遍历 / 坐标注入）、上传内容（哈希伪造 / 超大文件耗尽资源）、proxy 回源（上游投毒）、迁移导入（不可信来源）。M4 引入 Curation、完整性校验门、cosign 验签、限流与异常检测收敛攻击面。

## 2. 密钥与敏感数据

- **口令**：argon2 哈希存储，绝不明文；重置走受控流程。
- **API Token**：仅存摘要（不可逆），明文只在签发时一次性返回，不入库。
- **JWT 密钥**（`JIAN_JWT_SECRET`）**、上游凭据**：经环境变量 / Secret 注入，**严禁硬编码、严禁入库**；`.env` 在 `.gitignore` 中。
- **代理上游凭据**：以引用名 → 环境变量注入 `Authorization`，不落配置文件明文、不进日志。
- **日志脱敏**：令牌、口令、密钥、`Authorization` 头一律不进日志；错误信息不回显敏感值。

## 3. 外部输入

- **路径 / 坐标**：协议端点的路径、Maven 坐标、npm 包名严格校验并规范化，拒绝 `..`、绝对路径、符号链接逃逸；blob 落盘限制在可信工作目录根（`JIAN_TRUSTED_WORKDIR`）内。
- **内容完整性**：上传 / 回源边收边校验哈希，校验和不匹配即拒；大文件流式处理，设大小上限防资源耗尽。
- **上游内容**：proxy 缓存前校验；M4 起 digest 首见隔离 + Hash Pin + cosign 验签，未通过不对外提供。
- **注入防护**：SQL 经 sqlx 参数化，不拼接；管理 API 输入按 OpenAPI schema 校验（422）。

## 4. 漏洞报告

- 发现安全问题请通过项目维护者的私有渠道上报（安全邮箱 / 私有 issue），**请勿公开披露未修复漏洞**。
- 响应预期：确认接收后评估影响与修复计划；修复发布后在 CHANGELOG 标注（不含可被利用的细节）。
- 依赖漏洞由 CI 门禁（Go `govulncheck`、Node `osv-scanner`/`npm audit`）持续发现，随 `sdd-bump-dependencies` 处置。
