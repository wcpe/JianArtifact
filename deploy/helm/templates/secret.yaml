apiVersion: v1
kind: Secret
metadata:
  name: {{ include "jianartifact.fullname" . }}-secret
  labels:
    {{- include "jianartifact.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $k, $v := .Values.secrets }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
