apiVersion: v1
kind: Service
metadata:
  name: {{ include "jianartifact.fullname" . }}
  labels:
    {{- include "jianartifact.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "jianartifact.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
