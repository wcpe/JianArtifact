# 产品需求文档（PRD）：JianArtifact

> 需求的单一真源（WHAT / WHY），也是产品的**需求登记册 + 路线图**——全生命周期都在记，不是一次性文档。每个需求在 §4 加一行 FR（带优先级/期 + 状态），交付即标版本；§4 FR 表是迭代中最常动的部分（🔥 高频），分期（§7）只是其中很小、很静的一页粗线条规划。单功能的详细规格放 `docs/specs/`，PRD 只保留"一行 FR + 期 + 状态"的索引级。版本落地看 `docs/ROADMAP.md`。

## 1. 背景与目标

JianArtifact 是一个**自托管、单二进制交付的多格式制品仓库**（artifact repository），用 Go 重写，面向中小团队与私有化 / 离线环境。它托管与代理常见包格式（Raw/Maven/npm 起步，逐步扩展到 Docker/Cargo/PyPI/Go/NuGet 等），并提供从 **Nexus OSS 平滑迁移**的能力，让已有 Nexus 用户零丢失地切换。

一句话价值主张：**一个二进制、开箱即用、可从 Nexus 迁移的轻量私有制品库**——默认无需外部数据库 / 对象存储 / 中间件即可跑起来，同时保留向企业场景（OIDC/LDAP、S3、HA）演进的路径。

### 非目标

- 不做公共 SaaS 制品托管平台（面向私有化 / 自托管）。
- 不追求首版即覆盖全部包格式；按路线图分期扩展（见 `docs/ROADMAP.md`）。
- 默认形态不依赖外部数据库、消息队列、对象存储；这些为后续期的**可选**增强，需 ADR。
- 不做 CI/CD 流水线、代码托管、制品构建——只做制品的存储、代理、分发与治理。
- MVP 不做多节点高可用 / 分布式（留 M5 可选方案，先独立 ADR）。

## 2. 角色

- **管理员**：部署与运维实例、管理用户/仓库/ACL、执行 Nexus 迁移、配置代理上游与运行时。
- **开发者 / 消费者**：通过原生客户端（curl/mvn/npm 等）或管理端拉取、发布、浏览制品。
- **CI / 自动化系统**：以 API Token 鉴权，自动发布与拉取制品。
- **原生包客户端**：Maven、npm、curl 等按各格式协议与本系统交互，不感知本系统私有细节。
- **上游制品源**：被代理（proxy）的远程仓库（Maven Central、npmjs、Docker Hub 等）。

## 3. 用户故事

- 作为管理员，我希望首次启动即可经网页自举端点创建首个管理员账号（无需环境令牌），并在账号锁死时用 CLI 离线重置，以便零外部依赖地把实例跑起来并可恢复。
- 作为管理员，我希望把现有 Nexus OSS 的仓库与制品迁移进来（在线/离线/自有离线包三种来源），以便平滑切换且不丢数据。
- 作为开发者，我希望用原生 `mvn`/`npm`/`curl` 直接对接仓库发布与拉取，以便无需改变现有工作流。
- 作为开发者，我希望通过 group 仓库聚合多个 hosted/proxy，以便用一个地址取到所有依赖。
- 作为 CI 系统，我希望用 API Token 鉴权自动发布制品，以便无人值守地集成。
- 作为管理员，我希望按仓库设置可见性与读写 ACL，以便隔离不同团队的制品访问。
- 作为运维，我希望用单个二进制或一个容器就能部署、升级、备份与回滚，以便降低运维成本。

## 4. 功能需求（FR）

> 状态取值：计划 / 开发中 / 已交付@vX.Y.Z。优先级：P1(MVP=M1) / P2 / P3。
> 标 `已交付` 是有门的：只有该 FR 的 §6 / spec 验收标准全部满足、对应测试 / 实机验收通过后，才由 `sdd-release-version` 在发版时统一标 `已交付@vX.Y.Z`——开发 / 修复过程中不得自行预标。具体版本落地见 `docs/ROADMAP.md`。

### 4.1 M1 · MVP（P1，跨 0.1.0–0.5.0）

| 编号   | 需求                                                                                                   | 优先级 | 状态          |
| ------ | ------------------------------------------------------------------------------------------------------ | ------ | ------------- |
| FR-01  | monorepo 工程基座：pnpm workspace + Turborepo + Makefile + Go Task + go.work                           | P1     | 已交付@v0.1.0 |
| FR-02  | Docker 化质量门（格式/构建/静态分析/测试/契约一致/依赖漏洞），CI 与本地同一组脚本                      | P1     | 已交付@v0.1.0 |
| FR-03  | `api/openapi.yaml` 全域契约骨架 + `oapi-codegen` 生成 Go 接口/类型                                     | P1     | 已交付@v0.1.0 |
| FR-04  | 前端 client 据 OpenAPI 生成 + devmock 契约比对防漂移                                                   | P1     | 已交付@v0.1.0 |
| FR-05  | 单二进制交付：前端构建产物经 Go embed 内嵌，`CGO_ENABLED=0` 静态编译                                   | P1     | 已交付@v0.1.0 |
| FR-06  | 管理员自举（网页自举端点 + CLI 重置，无环境令牌）+ 口令 argon2id 哈希                                  | P1     | 已交付@v0.2.0 |
| FR-07  | 登录 / 会话 / 退出（JWT HS256）                                                                        | P1     | 已交付@v0.2.0 |
| FR-08  | API Token 签发 / 吊销，用于 CLI 与 CI 鉴权                                                             | P1     | 已交付@v0.2.0 |
| FR-09  | 用户管理、口令修改 / 重置                                                                              | P1     | 已交付@v0.2.0 |
| FR-10  | 仓库管理 + 可见性 / 读写 ACL                                                                           | P1     | 已交付@v0.2.0 |
| FR-11  | 管理端 web 全页面（Mantine 7 + i18next）对 devmock 完成                                                | P1     | 已交付@v0.2.0 |
| FR-12  | wiki UI 组件 / 业务模式验收站                                                                          | P1     | 已交付@v0.2.0 |
| FR-13  | Raw 协议：hosted + proxy + group                                                                       | P1     | 已交付@v0.3.0 |
| FR-14  | Maven 协议：hosted + proxy + group（含元数据）                                                         | P1     | 已交付@v0.3.0 |
| FR-15  | npm 协议：hosted + proxy + group                                                                       | P1     | 已交付@v0.3.0 |
| FR-16  | hosted/proxy 通用能力：浏览、使用片段、健康检查 / 就绪自检                                             | P1     | 已交付@v0.3.0 |
| FR-16a | 仓库文件树预览（左右分栏）+ 文件详情/下载/usage 复制；管理端 Raw hosted 上传；公开页 `/p/:name`        | P1     | 已交付@v0.4.0 |
| FR-17  | 并发下载单飞合并（single-flight）+ 大文件流式处理                                                      | P1     | 已交付@v0.3.0 |
| FR-18  | 内容寻址 blob 存储（文件系统）+ 校验和校验                                                             | P1     | 已交付@v0.3.0 |
| FR-19  | 原生客户端（curl/mvn/npm）互通真机验收                                                                 | P1     | 已交付@v0.3.0 |
| FR-20  | Nexus 迁移：三来源发现（在线 REST / 离线原生目录 / 自有离线包）                                        | P1     | 已交付@v0.5.0 |
| FR-21  | Nexus 迁移：计划预览 + 异步任务状态机                                                                  | P1     | 已交付@v0.5.0 |
| FR-22  | Nexus 迁移：持久化与幂等续传 + 冲突策略                                                                | P1     | 已交付@v0.5.0 |
| FR-23  | Nexus 迁移：凭据安全 + 迁移报告 + 切换与最终增量扫描                                                   | P1     | 已交付@v0.5.0 |
| FR-24  | 部署骨架：Dockerfile（多阶段）/ docker-compose / .env.example / 远程部署脚本                           | P1     | 已交付@v0.1.0 |
| FR-24a | Nexus drop-in URL 兼容与公开读入口（路径同形、public 匿名读、group 聚合、管理端 URL/成员列、运维说明） | P1     | 已交付@v0.4.0 |
| FR-25a | ACL 页面增强：用户下拉框（自动获取用户列表）+ 权限配置                                                 | P1     | 已交付@v0.4.0 |
| FR-25b | 制品详情增强：多校验和（SHA-256/SHA-1/MD5）登记 + 依赖坐标 + 下载/HTML View                            | P1     | 已交付@v0.4.0 |
| FR-25c | 后端 HTML 目录索引页（autoindex，浏览器访问仓库目录渲染列表）                                          | P1     | 已交付@v0.4.0 |
| FR-25d | 仓库列表增强：制品数/总大小统计展示                                                                    | P1     | 已交付@v0.4.0 |
| FR-25e | 仓库详情 Tab 布局（浏览/配置/ACL 集成）+ 浏览体验打磨                                                  | P1     | 已交付@v0.4.0 |

### 4.2 M1.5 · 体验优化与匿名访问（P2，0.6.0，滚动收纳）

> 0.5.0 后插入的大优化版本：收纳体验优化与增强需求（含后续新增），收口后整体验收发版（见 `docs/ROADMAP.md`）。

| 编号  | 需求                                                        | 优先级 | 状态 |
| ----- | ----------------------------------------------------------- | ------ | ---- |
| FR-30 | 全局跨仓库制品搜索（后端 API + Header 搜索栏）                    | P2     | 开发中 |
| FR-54 | 目录级制品列表 API（tree endpoint）+ 前端文件树懒加载        | P2     | 开发中 |
| FR-55 | 路由统一：公开页合并入主布局，未登录精简侧边栏             | P2     | 开发中 |
| FR-56 | 仓库列表增强：排序（名称/时间/制品数/大小）+ 分组（格式/类型）+ 创建时间 | P2     | 开发中 |
| FR-57 | 浏览页内制品搜索（仓库内路径过滤）                        | P2     | 开发中 |
| FR-58 | asset 表性能索引补齐（path 复合索引 + 跨仓库搜索优化）  | P2     | 开发中 |
| FR-59 | Header 增强：全局搜索栏 + 刷新当前页按钮                          | P2     | 开发中 |
| FR-66 | 内置 anonymous 主体 + ACL 可配置权限 + 实例级匿名访问全局开关     | P2     | 开发中 |
| FR-67 | 登录模态框化 + 取消强制跳转登录页 + 默认落地仓库列表              | P2     | 开发中 |
| FR-68 | 仓库列表页优化：固定布局（表格内滚）+ 操作列精简 + 匿名视图       | P2     | 开发中 |

### 4.3 M2 · 高频格式扩展 + 工程能力（P2，0.7.0）

| 编号  | 需求                                                        | 优先级 | 状态 |
| ----- | ----------------------------------------------------------- | ------ | ---- |
| FR-25 | Docker/OCI 协议：hosted + proxy + group                     | P2     | 计划 |
| FR-26 | Cargo sparse registry                                       | P2     | 计划 |
| FR-27 | PyPI（simple index）                                        | P2     | 计划 |
| FR-28 | Go modules proxy                                            | P2     | 计划 |
| FR-29 | NuGet                                                       | P2     | 计划 |
| FR-31 | 上述格式的 Nexus 迁移扩展                                   | P2     | 计划 |
| FR-32 | 声明式按格式启停（未启用格式零路由零后台任务）〔对标 NORA〕 | P2     | 计划 |
| FR-33 | 多架构发布起点：arm64 Linux 二进制〔对标 NORA〕             | P2     | 计划 |

### 4.4 M3 · 企业认证、存储与运维（P2，0.8.0）

| 编号  | 需求                                                                       | 优先级 | 状态 |
| ----- | -------------------------------------------------------------------------- | ------ | ---- |
| FR-34 | OIDC 认证                                                                  | P2     | 计划 |
| FR-35 | LDAP 认证                                                                  | P2     | 计划 |
| FR-36 | 用户组与更细粒度权限动作                                                   | P2     | 计划 |
| FR-37 | S3 兼容对象存储后端（需 ADR）                                              | P2     | 计划 |
| FR-38 | 审计日志                                                                   | P2     | 计划 |
| FR-39 | Prometheus 指标                                                            | P2     | 计划 |
| FR-40 | 备份与恢复                                                                 | P2     | 计划 |
| FR-41 | GC / 保留策略 / 配额 / 存储清理                                            | P2     | 计划 |
| FR-42 | 出站代理与受控运行时管理（含热重载）                                       | P2     | 计划 |
| FR-43 | 上游断路器（per-registry circuit breaker）〔对标 NORA〕                    | P2     | 计划 |
| FR-44 | 定时任务调度器（周期 GC/保留/压实/校验）〔对标 NORA〕                      | P2     | 计划 |
| FR-45 | 离线镜像 CLI（air-gapped lockfile 预取同步 `mirror`）〔对标 NORA〕         | P2     | 计划 |
| FR-46 | 存储后端迁移 CLI（local↔S3）+ `integrity verify` 离线全量校验〔对标 NORA〕 | P2     | 计划 |
| FR-47 | Helm Chart + Kubernetes 部署清单〔对标 NORA〕                              | P2     | 计划 |
| FR-48 | Windows 二进制补齐多平台发布矩阵〔对标 NORA〕                              | P2     | 计划 |

### 4.5 M4 · 安全、供应链与运营可见性（P2，0.9.0）

| 编号  | 需求                                                                                                                       | 优先级 | 状态 |
| ----- | -------------------------------------------------------------------------------------------------------------------------- | ------ | ---- |
| FR-49 | 多维限流、连接 / 并发限制、慢请求防护                                                                                      | P2     | 计划 |
| FR-50 | IP 名单、异常检测、应用层防护                                                                                              | P2     | 计划 |
| FR-51 | 本地漏洞库镜像与 Maven/npm/Cargo/PyPI/NuGet 坐标匹配                                                                       | P2     | 计划 |
| FR-52 | Docker 镜像层扫描                                                                                                          | P2     | 计划 |
| FR-53 | 使用分析、容量趋势、缓存命中率与运维告警                                                                                   | P2     | 计划 |
| FR-75 | Curation 内容治理链（blocklist/allowlist + namespace 隔离 + 最小发布年龄，enforce/audit 双模式，fail-closed）〔对标 NORA〕 | P2     | 计划 |
| FR-76 | 完整性校验门（Hash Pin：写入即钉、读取即验、失败即拒）+ 代理 digest 首见隔离〔对标 NORA〕                                  | P2     | 计划 |
| FR-77 | cosign 镜像 / 制品签名策略（上游拉取验签，不通过则阻断）〔对标 NORA〕                                                      | P2     | 计划 |

### 4.6 M5 · 长尾格式与成熟期能力（P3，0.10.0）

| 编号  | 需求                                                                          | 优先级 | 状态 |
| ----- | ----------------------------------------------------------------------------- | ------ | ---- |
| FR-78 | RubyGems、Conan、Terraform、Ansible Galaxy、Dart/Flutter Pub 等长尾格式       | P3     | 计划 |
| FR-79 | 高级虚拟仓库策略与跨成员 metadata 聚合                                        | P3     | 计划 |
| FR-80 | 多节点只读扩展或高可用可选方案（先独立 ADR）                                  | P3     | 计划 |
| FR-60 | 外部数据库 / 分布式存储可选方案（不破坏默认轻量形态，需 ADR）                 | P3     | 计划 |
| FR-61 | deb/rpm（apt/yum）Linux 系统包仓库〔对标 Nexus〕                              | P3     | 计划 |
| FR-62 | Webhooks（制品事件通知）+ 仓库内路径路由规则 / content selector〔对标 Nexus〕 | P3     | 计划 |

### 4.7 贯穿全期 · 产品供应链与发布质量

| 编号  | 需求                                                                         | 优先级 | 状态 |
| ----- | ---------------------------------------------------------------------------- | ------ | ---- |
| FR-63 | 签名发布（cosign / 校验和）+ 生成 SBOM + 接入 OpenSSF Scorecard〔对标 NORA〕 | P2     | 计划 |
| FR-64 | 对协议解析 / 路径校验等入口做 Fuzz 模糊测试并纳入 CI 门禁〔对标 NORA〕       | P2     | 计划 |
| FR-65 | 1.0.0-rc：全格式与迁移兼容矩阵回归 + semver 契约冻结 + 升级手册              | P3     | 计划 |

## 5. 非功能需求（NFR）

- **NFR-01 单二进制 / 零外部依赖**：默认形态无需外部 DB / MQ / 对象存储；一个二进制或一个容器即可运行。
- **NFR-02 大文件与流式**：上传 / 下载 / 回源全程流式，不整体入内存；支持数 GB 级制品，内存占用与文件大小解耦。
- **NFR-03 并发**：同一制品并发拉取经 single-flight 合并，回源至多一次；并发上传 / 下载在设定阈值内稳定。
- **NFR-04 性能**：常规制品拉取 P95 延迟与缓存命中率满足发布基线（数值在 ROADMAP 各版实测冻结）。
- **NFR-05 磁盘 / 内存边界**：blob 存储与缓存有明确上限与清理策略；迁移大规模仓库时磁盘占用可预估。
- **NFR-06 安全**：口令 argon2 哈希；令牌 / 凭据不入库不进日志；外部输入校验防注入与路径遍历；日志脱敏。
- **NFR-07 可维护性**：分层单向依赖、契约唯一真源、中文分级日志、静态分析警告即失败。
- **NFR-08 可移植性**：`CGO_ENABLED=0` 纯 Go；发布多平台（linux amd64 起，逐步 arm64 / Windows）。
- **NFR-09 兼容性**：与 Nexus OSS 3.70.x 三迁移路径兼容；与原生包客户端协议兼容。

## 6. 验收标准

整体"做完"的可验证判据，对齐测试；已知有实机 / 集成 / 真实环境维度的写成显式验收项并**须由用户确认通过**（测试绿不替代真的能用）。各版本的整期验收门在 `docs/ROADMAP.md` 逐版细化，此处为总纲：

- **AC-01（实机·须用户确认）**：原生 `curl` / `mvn` / `npm` 客户端对 Raw/Maven/npm 的 hosted+proxy+group 发布与拉取互通成功。
- **AC-02（实机·须用户确认）**：从真实 Nexus OSS 3.70.x 经三来源路径（在线 REST / 离线原生目录 / 自有离线包）迁移，制品与元数据零丢失、报告一致、可续传。
- **AC-03**：单二进制 / 单容器零外部依赖启动，`/readyz` `/healthz` 正常；管理员自举可用。
- **AC-04**：并发拉取 single-flight 合并、大文件流式的相关测试穷举通过（见 `testing-and-quality.md` §2）。
- **AC-05**：鉴权 / ACL 越权用例全部拒绝（401/403）；令牌 / 凭据不出现在日志与库中。
- **AC-06**：OpenAPI↔devmock 契约一致性检查通过；前后端据同一契约无漂移。
- **AC-07**：全部质量门（Go + 前端 + 供应链）在 Docker 与 CI 中同脚本通过。
- **AC-08（实机·须用户确认）**：Docker / Compose 部署演练成功，升级与回滚流程可执行。

## 7. 分期（路线）

各期只描述**主题 / 目标**；具体哪个 FR 属于哪期，以 §4 FR 表的优先级 / 期列为唯一来源；**具体版本落地见 `docs/ROADMAP.md`**（0.1.0 → 1.0.0-rc）。

- **第一期（M1，MVP）**：可迁移的核心制品库——认证授权 + Raw/Maven/npm 三格式 + Nexus 迁移 + 单二进制交付与部署骨架。
- **第一点五期（M1.5，插入）**：体验优化与匿名访问——MVP 后的大优化版本（滚动收纳优化需求：匿名访问、登录与列表体验、搜索与浏览增强等）。
- **第二期（M2）**：高频格式扩展（Docker/Cargo/PyPI/Go/NuGet）+ 声明式格式启停。
- **第三期（M3）**：企业认证（OIDC/LDAP）、S3 存储、审计 / 指标 / 备份、断路器 / 调度器 / 离线镜像 CLI、Helm/K8s。
- **第四期（M4）**：安全与供应链治理——限流防护、漏洞扫描、Curation、完整性校验门、cosign 签名。
- **第五期（M5）**：长尾格式（含 deb/rpm）、高级虚拟仓库、HA / 外部存储可选方案、Webhooks。

> 分期是粗粒度路线图横轴，数量很少；一期含很多 FR、跨很多版本。产品成熟（1.0 后）改按版本 + 功能组织，不再加"第 N 期"。某期是否完成看 §4 表里该期 FR 状态是否都 `已交付`。

## 8. 术语表

- **hosted 仓库**：本地托管、可发布制品的仓库。
- **proxy 仓库**：代理远程上游、按需回源缓存的仓库。
- **group 仓库**：聚合多个 hosted/proxy 成员、对外呈现单一地址的虚拟仓库。
- **blob**：制品的二进制内容，按内容哈希寻址存储于文件系统。
- **single-flight（单飞合并）**：同一制品的并发回源请求合并为一次上游拉取。
- **Curation**：对制品的准入治理（黑白名单、命名空间隔离、最小发布年龄等）。
- **Hash Pin（完整性校验门）**：制品写入时钉住哈希、读取时校验、不匹配即拒。
- **digest 首见隔离**：代理首次见到的 digest 先隔离待校验，通过后才对外提供。
- **Nexus OSS**：Sonatype Nexus Repository OSS，本项目提供从其迁移的能力。
- **devmock**：前端浏览器 + Node 双端 Mock，据 OpenAPI 契约比对防漂移。
