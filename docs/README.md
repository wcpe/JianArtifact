# JianArtifact 文档导航

本目录按“需求 → 现状架构 → 接口 → 运维 → 决策 → 功能规格”组织。先看本页，再进入对应真源；不要把历史规格或临时验收笔记当作当前实现说明。

## 1. 先看什么

| 目的                        | 真源                                 | 说明                                                                     |
| --------------------------- | ------------------------------------ | ------------------------------------------------------------------------ |
| 当前需求、FR 状态和版本归属 | [`PRD.md`](PRD.md)                   | 只登记 WHAT/WHY 和一行 FR，不写实现过程                                  |
| 当前代码架构                | [`ARCHITECTURE.md`](ARCHITECTURE.md) | 描述已经落地的模块、数据、依赖和边界；未落地目标单独标注                 |
| 管理 API 总览               | [`API.md`](API.md)                   | 只写概览；字段和路径以 [`../api/openapi.yaml`](../api/openapi.yaml) 为准 |
| 版本与整期门禁              | [`ROADMAP.md`](ROADMAP.md)           | 说明版本主题、验收门和发布状态                                           |
| 部署、升级、恢复和排障      | [`OPERATIONS.md`](OPERATIONS.md)     | 只写当前可执行流程；未完成真实发布验收的能力必须标明状态                 |
| 为什么这样设计              | [`adr/README.md`](adr/README.md)     | ADR 是决策史；被取代的决策只供追溯                                       |
| 功能开发细节                | [`specs/README.md`](specs/README.md) | 规格按功能归档，历史规格不代表当前代码                                   |

## 2. 当前版本口径

- 版本真源是仓库根目录 [`../VERSION`](../VERSION)，当前发布版本为 `0.8.0`（release 提交 + 附注 tag `v0.8.0`）。
- 当前开发窗口是 `0.9.0`（M3：企业认证、存储与运维）；已发布版本的 FR 状态以 [`PRD.md`](PRD.md) 为准。
- 运行时是**单实例独立部署**：实例本地持有自己的 SQLite 与 blob，不共享数据库、文件卷或进程内状态，也没有节点角色、拓扑或复制通道。
- 节点搬迁/备份以**一致性备份包**为传输单位：生成 → 传输 → 导入 → 重启替换 → 校验（见 [`OPERATIONS.md`](OPERATIONS.md) §3.2/§3.3/§5）。0.7.0 的全互联双向复制、0.8.0 的主备级联复制均属历史交付，已随 FR-138 整体退役（决策见 [`adr/0027`](adr/0027-package-based-node-backup-and-relocation.md)），新部署不得按它们配置。

## 3. 当前搬迁与备份形态

```text
旧实例（唯一数据边界：SQLite + 内容寻址 blob）
        │ 1. 生成备份包（热备份 / 冻结窗口）
        │ 2. 传输（签名链接无登录下载 / 人工拷贝）
        ▼
新实例（独立数据目录）
        │ 3. 导入（CLI / URL 拉取 / 分片上传）→ 暂存 + restore.pending
        │ 4. 重启替换（先留 pre-restore-<ts>/ 回滚点）
        └── 5. 校验规模与登录 → 切 DNS
```

切换窗口可用**增量差包**压缩到分钟级（冻结 → 生成差包 → 传输 → 导入 → 起服 → 解冻）。包内**只含数据、不含任何密钥或节点本地配置**，因此新实例导入后必须自行配置密钥与对外地址。详细流程见 [`OPERATIONS.md`](OPERATIONS.md) §3 与 §5，包格式见 [`specs/0.8.0-node-backup-and-relocation.md`](specs/0.8.0-node-backup-and-relocation.md)。

## 4. 文档状态标记

- **现状**：已经由当前实现和当前可复核配置支持。
- **开发中**：已登记并写入规格，但代码、契约或真实验收仍未完成。
- **开发完成，待用户验收**：代码与开发验证完成，但仍缺用户环境要求的真实客户端、浏览器或多节点证据。
- **历史**：曾经交付或曾经采用，当前不作为新部署依据。
- **待发版**：实现和规定范围验收完成，但尚未创建正式版本提交、tag、CI 或 Release。

临时计划、审查报告和对账工单放在仓库 `.tmp/`，不属于正式文档真源。
