// 文件树渲染共享常量（FR-115）：仓库详情树（FR-93）与搜索结果树（FR-94）共用同一缩进步长，
// 保证两处文件树「层级递进」观感一致，避免各自散落魔法值。

/**
 * 每深一层的缩进像素步长。
 *
 * 取值偏大（24px）以让 top/wcpe/test/1.0.0 等多级目录在视觉上明显递进，
 * 而非各层挤在相近缩进（修 FR-115「没有层级递进」反馈）。
 */
export const TREE_INDENT_STEP = 24;
